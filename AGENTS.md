# Global Agent Instructions

## このプロジェクトでは特に重要なルール

- Coreにビジネスロジックを集約し、CLIやVSCode拡張はCoreを呼び出すだけにする。
- MermaidやPlantUMLのレンダリング処理と、PDF上のレイアウト処理は分離する。
- TypeScriptはstrictモードを前提とし、anyは使用しない。
- 依存関係は一方向に保ち、循環参照を作らない。
- 生成物はすべてgenerated/配下に出力し、Git管理しない。

## コーディングルール

- TypeScript Strict Mode
- anyは禁止
- interfaceを優先
- Promiseはasync/awaitのみ
- 関数は単一責務
- 1ファイル300行以内を目安
- 依存は一方向
- コメントより命名で意図を表現
- テスト可能な設計を優先

## Safety Defaults

- 本番環境に影響するコードを修正する前に確認を求める
- 秘密情報（APIキー、パスワード等）はコードに直接書かない
- 大量のファイル削除前に確認を求める

## 禁止事項

- 今回指示していない機能を追加しないこと。
- ライブラリを勝手に追加しないこと。
- 将来を見越した未使用コードを書かないこと。
- TODOだけのコードを書かないこと。
- 実装を省略したダミーコードを書かないこと。
- エラーを握り潰さないこと。
- CLIにビジネスロジックを書かないこと。
